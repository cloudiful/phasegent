{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the phase-1 single-binary shell. No core privileged APIs; only the redacted read-only snapshot command is exposed via invoke.",
  "windows": ["main"],
  "permissions": []
}
